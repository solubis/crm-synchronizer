Information
=============

Tests are executed via /test/test.html
Server URL configuration (acrm.data.serverURL) is in test case file /test/test-synch.js
For testing with files instead of services set acrm.data.useFiles to True. Sample files with JSON responses are id /db directory
